def fibonacci_huge_naive(n, m):
    if n<=1:
        return 1
    
    previous = 0
    current = 1
    sum 

    for _ in range(n-1):
        previous, current = current, previous + current

    return current%m